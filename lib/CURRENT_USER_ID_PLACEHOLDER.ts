/**
 * A string constant that represents a placeholder for the currently logged-in user.
 * Useful when the current user Id needs to be passed to a graphql query as a variable,
 * but we don't want to wait for a prior query to get the real Id.
 */
export const CURRENT_USER_ID_PLACEHOLDER = "currentUser";
