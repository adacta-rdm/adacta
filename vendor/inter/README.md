# Inter

Version 4.1, from <https://rsms.me/inter/>. `InterVariable.woff2` is the upright
variable font as published, covering every weight from 100 to 900.

The same place publishes an italic and a file per weight. The application uses
neither.

Used under the SIL Open Font License. `LICENSE.txt` comes from the same
distribution.
